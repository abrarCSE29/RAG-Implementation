# import torch
# print(torch.cuda.is_available())  # Should return True if CUDA is set up correctly
# print(torch.version.cuda)
# import torch
# torch.cuda.empty_cache()
export CUDA_VISIBLE_DEVICES=0  # or whatever GPU index you want to use
